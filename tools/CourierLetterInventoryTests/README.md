# Courier letter inventory checks

`run.py` checks production action-tag stripping, visible-text sanitization,
bounded/delayed restore retries, and public detail lookup. Real inventory and
save migration remain `LIVE/SAVE=NOT-RUN`.
