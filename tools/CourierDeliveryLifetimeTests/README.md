# Courier delivery lifetime checks

`run.py` checks production return, payload refund, destroyed/missing handling,
runtime-index cleanup, and final party teardown ordering. Game inventories,
parties, notifications, and old saves remain `NOT-RUN`.
