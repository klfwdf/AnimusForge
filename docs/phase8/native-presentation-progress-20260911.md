# Native 展示回调作用域续作（2026-09-11）

起点 36e04059，生产基线 77d4a940。fetch 后同名远端 ahead 8 / behind 0；不合并、不推送，保留两份用户草稿。

## 本轮范围

现有后端准入保护不等于 UI 队列结果归属。流式/完成/错误回调在后台入队后，Overlay 只检查提交编号和当前对象可用性，当前对象变为另一个合法 NPC 时仍可能显示旧内容。

新增内部展示观察票据，保留 save generation、conversation epoch、会话/目标与独立展示修订号；观察票据可在后端 Task 正常释放后继续验证最终显示，但不能执行游戏动作。普通输入/主动开场复用同一内部桥和完整旧 Native 流程；V1 仍不开放提交。

UI 实际消费队列、等待动画、后处理提示、最终文本/错误与收尾均区分“本界面提交仍拥有本地状态”与“仍有权操作当前对话显示”。失效时只释放本地旧 busy，不写旧回复/旧原文、不结束新流式、不改新请求。Tick 只用轻量身份戳，不加实体全量扫描；完整目标检查放在已有显示边界。

不改额外模块业务、默认管线、TTS 物理取消、存档或游戏安装。完整 Native prepare/原子事实回执、Courier prepare 仍待后续。本页作为编辑前检查点；验证包含原回调行为反例、候选行为/变异、现有准入回归与 Stage 构建。

## 已实现

- 加入内部 `NativeConversationPresentationScope`：origin 捕获不占用后端；一次提交后绑定真实准入快照和展示 revision；后端释放后仍能验证最终显示。
- 原普通/主动两条 UI 提交改经同一个内部观察桥，继续调用原完整 Native 后端；不是迁移到新 detached 默认路径。
- 14 个 UI 异步消费位置统一出队验证；等待点动画、后处理通知与最终收尾也使用捕获范围。
- Tick 只做轻量身份戳；过期时递增本地 UI generation，释放本地 busy、等待和提示，不恢复旧文本或结束新流式。
- 删除重复的“当前 NPC 是否可用”分支及其中会把旧原文写入新 NPC 的 fallback。旧 public ForExternal 重载/查询保留兼容，不再用于 Overlay 的结果归属判断。

## 验证与继续

原队列串显示已复现；46 个新展示检查 / 6 个展示变异通过，原 44 个准入检查 / 7 个变异保留。六项 Debug/Release Stage 构建与相关回归见最终审计记录。未做真实游戏、存档或新子 MOD 验收；未推送、未部署。

下一项仍是 Native prepare/动作后事实回执及剩余 TTS 直接回调边界，然后继续 Courier 双向 prepare。公共 V1 仍只读，自动化继续，不把本轮 Overlay 保护视为整个 Native 已交付。

本轮生产/测试已本地提交 `32230a64`；回滚检查点 `ee6b7baf`。最终 6 项构建与相关回归通过，准确证据见 `docs/audits/2026-09-11-native-presentation-verification.json`。自动化继续，未实机验收、未推送、未覆盖游戏。
