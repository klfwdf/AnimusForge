# Native 展示回调作用域续作（2026-09-11）

起点 36e04059，生产基线 77d4a940。fetch 后同名远端 ahead 8 / behind 0；不合并、不推送，保留两份用户草稿。

## 本轮范围

现有后端准入保护不等于 UI 队列结果归属。流式/完成/错误回调在后台入队后，Overlay 只检查提交编号和当前对象可用性，当前对象变为另一个合法 NPC 时仍可能显示旧内容。

新增内部展示观察票据，保留 save generation、conversation epoch、会话/目标与独立展示修订号；观察票据可在后端 Task 正常释放后继续验证最终显示，但不能执行游戏动作。普通输入/主动开场复用同一内部桥和完整旧 Native 流程；V1 仍不开放提交。

UI 实际消费队列、等待动画、后处理提示、最终文本/错误与收尾均区分“本界面提交仍拥有本地状态”与“仍有权操作当前对话显示”。失效时只释放本地旧 busy，不写旧回复/旧原文、不结束新流式、不改新请求。Tick 只用轻量身份戳，不加实体全量扫描；完整目标检查放在已有显示边界。

不改额外模块业务、默认管线、TTS 物理取消、存档或游戏安装。完整 Native prepare/原子事实回执、Courier prepare 仍待后续。本页作为编辑前检查点；验证包含原回调行为反例、候选行为/变异、现有准入回归与 Stage 构建。
