# 通用主线程函数修复检查点（2026-09-11）

基线 `613ac245`；仅修改 Native/Scene 共用调度函数及定向测试。先复现已开始操作被超时放弃、诊断改变结果、前处理异常在排队路径被吞的问题。

保留所有调用者及普通异常 fallback 业务策略；仅未开始操作可过期，已开始等待真实结果。保持前处理格式异常向原上层失败处理传播。日志不拥有完成结果，不引入另一套队列/公开 API。

不推送、不部署、不操作真实存档、不修改制作组业务和两份 2026-09-06 用户草稿。验证后本地提交，更新总交接及简明交接。
