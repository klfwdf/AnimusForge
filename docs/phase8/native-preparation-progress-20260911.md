# Native 初始场景准备检查点（2026-09-11）

基线 `50f84818`。本轮只把 persona await 之后、历史/前处理任务之前的 NPC / 场景目标 / 会面挑衅规则 / 规则资格准备放进原主线程队列，同次消费先验证 admission。保留原函数、参数和输出，不改制作组业务。

持久历史链已确认包含记忆召回/前处理选择和对可变记忆集合的读取，不能整段搬到主线程；该深层快照后续处理。本轮也不宣称 persona 网络、TTS 或 Courier 已完成。

先执行能失败的原源码片段测试与健康输入对照，再改源码，更新独立验证和精确 SHA 白名单。只本地提交及 Stage，不推送/部署/存档访问，保留用户草稿。
