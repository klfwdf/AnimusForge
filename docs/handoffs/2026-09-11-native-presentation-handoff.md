# Native 展示隔离：制作组短版 HANDOFF

这轮继续修复“旧回复串到新对话”的问题。

- 现在显示回复时会核对它原本属于哪次会话，而不只看当前 NPC 是否还有效。
- 换 NPC、读档、关闭重开或新请求开始后，旧排队内容、等待点和行为提示不会再写入新对话。
- 后端生成结束后，正常的最终回复仍可显示；不会因为后台忙碌已释放就误删正常结果。
- 过期请求只释放它自己的界面忙碌状态，不清新请求、不还原旧 NPC 文本、不结束新流式。

仍走原有完整 Native 流程，政策/宴会/GCCZ 业务没改，新公共 API 仍只读。没有推送或覆盖游戏。

本轮有 46 项新展示检查和 6 个变异控制；原 44 项准入检查、7 个变异保留。详细构建/回归和提交见总 HANDOFF。**真实游戏与旧存档仍需测试，不能称整个重构已完成。**

下一步继续 Native 线程/事实回执和剩余 TTS 边界，再处理 Courier 早期准备流程。

本轮生产/测试已本地提交 `32230a64`；回滚检查点 `ee6b7baf`。最终 6 项构建与相关回归通过，准确证据见 `docs/audits/2026-09-11-native-presentation-verification.json`。自动化继续，未实机验收、未推送、未覆盖游戏。
