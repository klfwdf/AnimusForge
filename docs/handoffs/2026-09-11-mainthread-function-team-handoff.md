# 给制作组的简明 HANDOFF

## 这次改了什么

修了自由对话和场景喊话共用的排队执行逻辑：没开始的超时任务不会之后偷偷补做；已经开始的操作会等真实结果，不再提前按失败放弃。日志出错也不会改变对话结果，前处理格式错误会正常交给原失败提示处理。

## 没改什么

25 个业务调用点保持原样，没有改政策、宴会、GCCZ 玩法/数值、存档格式或默认渠道。子 MOD 新 API 仍只读。

## 验证情况

132 项定向检查、7 个缺陷变异、相关三渠道回归和 1.3/1.4/Bootstrap 的 Debug/Release 构建通过。**这是代码和离线验证，不是新版本实机验收。**

## 后续

继续收拢 Native 更早的人设/规则/持久记忆准备与 TTS 回调，再处理信使双向准备。整个阶段 8 还不能写 DONE。

之后获准部署时，请重点看：正常对话与主动开场、多人接力/旁听、直接指令、慢请求期间切场景/读档后是否出现重复动作或错人记录。没有复现到对应慢请求场景就记“未覆盖”，不要直接打 PASS。

本地代码提交 `5bf830f3`，检查点 `84d7097b`。未推送 GitHub、未覆盖游戏、未操作真实存档。总交接见仓库根目录 `HANDOFF.md`。
