# AF 重构进展：Scene 整模块修复

## 现状

阶段八继续推进。本轮完成了 Scene 默认链路的项目内修复与离线验证，**不是全阶段交付完成，也尚未部署游戏**。

## 已完成

- 主链路只生成正文，取消提前执行动作和记忆。
- 完整后处理重新统一负责规则、动作与多人接力，保留真实候选及首轮/接力差异。
- 恢复旁听 NPC 的原有记忆入口，避免重复记录玩家发言。
- 防止正文泄漏的动作标签绕过后处理执行。
- 补齐读档、离场、超时、旧回调和新场景等待状态的隔离。
- 132 项边界回放、71 组后处理差分、37 组 Queue、6 组 Gate 时序通过；1.3/1.4/Bootstrap 的 Debug/Release 六项构建通过。

## 尚未完成

- 真实游戏、旧存档、金币/物品/债务、AFEF 和 TTS 的本轮版本验收。
- 全局 Bridge 清单有一处与另一项安全补丁修复不一致，已记录，未假报通过。
- Native 完整等价、前段 capture 线程边界、BattleSpeech 特殊回退时序及全领域最终回归。

## 制作组下一步

重点复核：多人接力与旁听记忆、动作不重复、跟随/传唤结束、生成中读档/退出及 API 超时。请提供**测试提交号、游戏版本、操作、预期与结果日志**；过去版本的验收不能自动覆盖这轮。

保留仍有调用的旧接口，不为“删干净”牺牲功能。代码和完整 HANDOFF 已纳入本轮本地交付，未推送、未覆盖游戏；阶段八仍不能标 DONE。
