# AF 主体续作：记忆接受结果（制作组短版）

## 这轮改了什么

以前，记忆 owner 缺失、写入返回失败或内部报错，都可能继续按正常回复结束。

现在 Native 会检查明确的记忆接受结果：
- 保留原场景编号、Hero/非 Hero 资格及完整对话内容。
- 记忆未确认时明确提示，不假装整轮成功，不自动重试或删除已经写入的部分记录。
- 动作已要求关闭对话时，保留原会话的关窗请求，不因此卡住或误关新对话。
- 原六参接口继续兼容，新 scene-aware 入口仅内部使用；新 Api.V1 仍只读。

原动作规则、数值、AFEF 格式和底层存储实现没重写，没有再造一套记忆系统。

## 验证

184 项连通检查、15 个反例变异通过；原 Native/Scene/Courier/制作组桥接回归通过。1.3、1.4、Bootstrap 的 Debug/Release 六构建通过；实际四份 DLL 的 532 个元数据检查通过。

本地实现：`18f48678`。总入口：`HANDOFF.md`。

## 尚未完成

运行期“已接受”不代表游戏已存盘，也不是动作和记忆的完整原子事务。更早准备/失败清理、TTS、Courier 与实机/旧存档验收还要继续。

**未推送 GitHub、未覆盖游戏、未进行本轮实机验收；整个重构还未 DONE。** 自动化继续做项目内工作。
