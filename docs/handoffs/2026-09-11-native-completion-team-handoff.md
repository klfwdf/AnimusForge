# AF 主体续作：制作组简明交接

## 这轮完成

- Native 动作后的历史记录改到主线程，不再「先检查、再回后台写」。
- 提前固定原场景和非 Hero 部队记忆身份，避免旧回复写进新场景/新部队。
- 动作合法结束对话后，保留原目标的历史派发；不会借旧回调改新界面或关闭新对话。
- 动作被丢弃时，只在原上下文清理 pending 玩家记录，不盲删新存档历史。

原玩法、数值、标签与制作组模块业务没有重写；移除了对应旧后台收尾路径，不是旁边再加一套。

## 验证

102 项新检查、9 个反例变异通过；原 Native/Scene/Courier、模块桥接及相关回归通过。1.3、1.4、Bootstrap 的 Debug/Release 六构建通过，只有项目内 Stage。

本地代码：`d7ab9610`。总入口：`HANDOFF.md`；详细边界：`docs/architecture/af-native-completion-boundary.md`。

## 仍需注意

**未推送、未覆盖游戏、未进行本轮实机或旧存档验收；整个重构尚未 DONE。** 新 API.V1 仍只读。历史 owner 目前仍缺可靠接受回执，不能把方法正常返回说成 AFEF/存档已保存。

下一步补历史接受结果及更早准备/失败清理，再推进 TTS、Courier。制作组后续实测重点：正常/主动开场、无发言、换人/换场景/读档、进大厅/带路/组建部队后的历史与关窗；金币、物品、债务、AFEF 按实际效果对照。
