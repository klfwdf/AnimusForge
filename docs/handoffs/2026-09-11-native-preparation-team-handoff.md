# 给制作组的简明 HANDOFF

## 这次做了什么

自由对话开始时，要准备 NPC 信息、场景传唤/带路对象和规则资格。这一段以前在后台读游戏状态，现在改成：**先确认还是原会话，再在主线程一次准备好**。会话失效或未开始就超时，不再继续这段准备。

原函数、参数和结果保留，没有改人设文本、玩法、数值、政策/宴会/GCCZ 业务或存档格式，也没有切换默认入口。

## 验证到哪里

589 项定向检查、5 个缺陷变异、原准入/调度测试、三渠道相关回归和 1.3/1.4/Bootstrap 的 Debug/Release 构建通过。**没有进行本轮实机验收，也没有覆盖游戏。**

## 还没完成

更深的持久记忆构造里还有检索与前处理，不能直接整个搬到主线程；这是下一步。之后继续 TTS 和信使准备。新子 MOD API 仍只读，整个阶段 8 还不能标 DONE。

后续获准部署后，制作组请重点对照：Hero 与普通 NPC、城内/野外会面、传唤和带路对象、挑衅规则、主动开场，以及等待时切场景/读档的晚返回。主线程耗时也需实机观察。

本地代码 `0306beba`；检查点 `62468e7c`。未推送 GitHub、未部署、未操作真实存档。总状态见根目录 `HANDOFF.md`。
