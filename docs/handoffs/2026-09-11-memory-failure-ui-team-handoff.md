# 给制作组的简明 HANDOFF

## 这次修了什么

记忆召回/筛选或日结总结报错时，不再让后台线程直接弹游戏窗口。现在由当前战役的主线程显示，重复报错合并，过期存档操作的提示被拦住，旧窗口的确认也不会误清新窗口。

原 9 处错误文字、按钮、暂停方式、召回算法和重试规则没改。没有改政策、宴会、GCCZ 业务，也没有切换默认渠道。

## 验证情况

85 项定向检查、7 个缺陷变异、相关回归及 1.3/1.4/Bootstrap 的 Debug/Release 构建通过。存档契约仅刷新两处代码行号，168 个绑定身份不变。

## 还没完成

**这只是记忆失败提示的线程和生命周期修复，不是整个记忆系统改完。** 可变记忆数据、当前场景/日期及身份读取仍是下一步，之后继续 TTS 和信使准备。新子 MOD API 仍只读，阶段 8 未完成。

后续获准部署后，请测试记忆 API/ONNX 失败提示、确认后再次报错、连续错误、等待时读档或退回菜单；注意不能把本地模拟测试当成实机通过。

本地代码 `6f0bac67`；检查点 `88777e45`。未推送、未部署、未操作真实存档。总交接见根目录 `HANDOFF.md`。
