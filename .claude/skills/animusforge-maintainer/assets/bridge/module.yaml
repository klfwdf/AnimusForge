# Optional declaration example, not an installed runtime schema.
# Use only when a real consumer exists; adapt to its verified schema.
# Ordinary internal extraction does not require this file.
id: "af.bridge.__ID__"
kind: bridge
owner:
  team: "__OWNER__"
  maintainers: ["__MAINTAINERS__"]
compatibility:
  bannerlord: ["1.3", "1.4"]
# Conditional: actual module/contract versions, entryType, dependencies,
# capabilities, profiles and lifecycle effects accepted by the consumer.
# Declare persistence only when state exists; preserve legacy keys/types.
# A bridge also documents both participating owners and supported contracts.
